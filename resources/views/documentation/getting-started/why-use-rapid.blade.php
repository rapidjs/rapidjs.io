@include('components.heading', ['type' => 'h2', 'name' => 'why-use-rapid', 'title' => 'Why Would I Use Rapid?'])

<p>In my experience, most of my apps used similar requests. I needed to <code class="language-js">.create()</code>, <code class="language-js">.find()</code> a model or collection, and <code class="language-js">.update()</code> or <code class="language-js">.destroy()</code> a model. With almost no configuration those routes are available to you in with simple methods.</p>

<ul>
    <li>Simplicity.</li>
    <li>Reusability.  {{-- If you're making these requests in multiple places across your site/apps, rapid is re-usable --}}</li>
    <li>Flexability.</li>
    <li>Extendability.</li>
</ul>
